import styled from 'styled-components';

export const





/*import propTypes from 'prop-types';MyRoute.defaultProps = {
  isClosed: false,
}

MyRoute.propTypes = {
  component: propTypes.oneType([propTypes.element, propTypes.func]).isRequired,
  isclosed: propTypes.bool,
};*/